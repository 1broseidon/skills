# Examples

Human-only examples and reference material for scribe. **Do not auto-load these during normal runs** — they are illustrative, not part of the skill's decision path.

Planned for v0.2:

- A `scribe audit` of a real README, with findings and axis scores
- A `scribe sync` run showing a populated drift ledger
- A `scribe extract` scaffold from a CLI source tree
- A before/after showing mode-mixing fixed (a reference that stopped teaching)

See [ROADMAP.md](../ROADMAP.md).
