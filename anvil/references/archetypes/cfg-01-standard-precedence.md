# CFG-01 · flags → env → file → defaults

Print in root help:

```
Configuration (highest wins):
  1. Command-line flags
  2. Environment variables (prefix: EXAMPLE_)
  3. Config file (--config, default paths: ...)
  4. Built-in defaults
```

List env vars in `conventions.yaml` with type and default.
