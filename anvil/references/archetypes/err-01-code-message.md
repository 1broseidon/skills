# ERR-01 · Code + message JSON

```json
{ "code": "INVOICE_LOCKED", "message": "Invoice inv_123 is locked." }
```

Optional `details` object for field errors. Use for CLIs (stderr) and internal JSON APIs.

Stable `code` enum documented in `conventions.yaml` or OpenAPI enum.
